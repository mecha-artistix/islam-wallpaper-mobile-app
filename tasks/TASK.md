# Task: <name>

## Goal
<one or two sentences — what this task achieves>

## Scope
<what files/features are in scope>

## Out of scope
<what is explicitly NOT touched — prevents accidental scope creep>

## Acceptance criteria
- [ ] <criterion 1>
- [ ] <criterion 2>
- [ ] <each criterion is testable>

## Testing required
- [ ] `npm run lint` passes (0 errors)
- [ ] `npm test` passes (new tests added for new logic)
- [ ] Manual checks from `tests/MANUAL-CHECKLIST.md` (list which)
- [ ] OS-level background behavior verified on device OR reported as NOT TESTED with reason

## Relevant feature docs
- `docs/features/<feature>.md`
- <any cross-feature docs>

## Notes
<any context a sub-agent needs>
